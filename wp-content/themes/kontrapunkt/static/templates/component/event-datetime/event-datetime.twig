{#
name: "Event Datetime"
#}
{% import '@macro/icons/icons.twig' as icons %}
{% extends "@component/component/component.twig" %}
{% set component = {
  'name': 'event-datetime',
  'id': content.wrapper_id,
  'classes': [
    content.wrapper_classes,
  ],
} %}

{% block content %}
<div class="space-y-2 text-base">
  {% if content.date_start and not content.date_end %}
    <div class="flex flex-wrap gap-2 shrink-0 items-center">
      {{ __('from', 'kontrapunkt') }}:
      <div class="text-gray-900 leading-tight">{{ content.date_start|date('j. n. Y') }}</div>
      {% if content.time_start is not empty %}
        - <div class="text-gray-900 leading-tight">{{ content.time_start|date('H:i') }}</div>
      {% endif %}
    </div>
  {% endif %}
  {% if content.date_start and content.date_end %}
    <div class="flex flex-wrap gap-2 shrink-0 items-center">
      {{ __('from', 'kontrapunkt') }}:
      <div class="text-gray-900 leading-tight">{{ content.date_start|date('j. n. Y') }}</div>
      {% if content.time_start is not empty %}
        - <div class="text-gray-900 leading-tight">{{ content.time_start|date('H:i') }}</div>
        {% if content.additional_showtimes %}
          {% for showtime in content.additional_showtimes %}
            {% if showtime.time_start is not empty %}
              / <div class="text-gray-900 leading-tight">{{ showtime.time_start|date('H:i') }}</div>
            {% endif %}
          {% endfor %}
        {% endif %}
      {% endif %}
    </div>

    <div class="flex flex-wrap gap-2 shrink-0 items-center">
      {{ __('to', 'kontrapunkt') }}:
      <div class="text-gray-900 leading-tight">{{ content.date_end|date('j. n. Y') }}</div>
      {% if content.time_end is not empty %}
        - <div class="text-gray-900 leading-tight">{{ content.time_end|date('H:i') }}</div>
        {% if content.additional_showtimes %}
          {% for showtime in content.additional_showtimes %}
            {% if showtime.time_end is not empty %}
              / <div class="text-gray-900 leading-tight">{{ showtime.time_end|date('H:i') }}</div>
            {% endif %}
          {% endfor %}
        {% endif %}
      {% endif %}
    </div>
  {% endif %}
</div>
{% endblock %}
